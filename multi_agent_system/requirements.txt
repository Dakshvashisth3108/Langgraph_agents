# ---------------------------------------------------------------
# requirements.txt — minimal dependency set for the multi-agent system.
# Tested with Python 3.10+ and Ollama running qwen3:8b locally.
# ---------------------------------------------------------------

# Orchestration layer
langgraph>=0.2.50,<0.3.0

# Core LangChain framework
langchain>=0.3.7,<0.4.0
langchain-community>=0.3.5,<0.4.0

# Ollama integration (talks to the local qwen3:8b server)
langchain-ollama>=0.2.0,<0.3.0

# Frontend
streamlit>=1.39.0,<2.0.0

# Environment / config
python-dotenv>=1.0.1,<2.0.0
